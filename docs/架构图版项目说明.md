# Crawl_cc 架构图版项目说明

## 1. 文档目的

本文档是 `Crawl_cc` 的“架构图版”说明，面向以下场景：

- 快速理解项目整体结构
- 做方案汇报或演示
- 作为阅读源码前的导航图
- 辅助技术评审和项目交接

如果你需要更完整的文字版说明，请结合仓库根目录下的 `项目说明文档.md` 一起阅读。

## 2. 项目一句话概括

`Crawl_cc` 是一个围绕懂车帝汽车车系数据构建的工程化 RAG 项目，覆盖“采集 -> 清洗 -> 治理 -> 分流 -> 建索引 -> 问答 -> 评测 -> 回流 -> 工作流编排 -> 多轮对话服务”完整链路。

## 3. 总体架构图

```mermaid
flowchart LR
    A[懂车帝页面] --> B[采集层 scraper]
    B --> C[raw HTML / raw JSON]
    C --> D[清洗层 cleaner]
    D --> E[cleaned JSON / Markdown]
    E --> F[治理层 agent_pipeline]
    F --> G[processed JSONL / Parquet]
    G --> H[资产分流 dataset_splitter]
    H --> H1[rag_corpus]
    H --> H2[training_corpus]
    H --> H3[eval_candidates]
    H1 --> I[向量构建 rag_llm_demo build]
    I --> J[FAISS / records / meta]
    J --> K[RAG 检索问答]
    K --> L[离线评测 rag_eval / rag_ragas_eval]
    L --> M[反馈回流 feedback_bridge]
    M --> N[review / repair / feedback 队列]
    G --> O[多轮对话 conversation]
    J --> O
    O --> P[FastAPI + Web UI]
    Q[workflow 脚本] --> B
    Q --> I
    Q --> L
    Q --> M
```

## 4. 分层架构图

```mermaid
flowchart TB
    subgraph L1[采集层]
        S1[dcd_scraper.py]
    end

    subgraph L2[清洗层]
        C1[dcd_cleaner.py]
    end

    subgraph L3[质量与治理层]
        Q1[data_quality.py]
        G1[GovernanceOrchestrator]
        G2[rules_bridge.py]
        G3[agents.py]
        G4[llm_agents.py]
    end

    subgraph L4[语料与索引层]
        A1[dataset_splitter.py]
        A2[llm_processor.py]
        A3[faiss_vector_store.py]
        A4[light_storage.py]
    end

    subgraph L5[RAG 与评测层]
        R1[rag_demo.py]
        R2[rag_llm_demo.py]
        R3[rag_eval.py]
        R4[rag_ragas_eval.py]
    end

    subgraph L6[多轮对话服务层]
        D1[service.py]
        D2[dialog_router.py]
        D3[query_rewriter.py]
        D4[rag_backend.py]
        D5[api.py]
        D6[memory/store.py]
    end

    S1 --> C1
    C1 --> Q1
    C1 --> G1
    G2 --> G1
    G3 --> G1
    G4 --> G1
    G1 --> A1
    G1 --> A2
    A1 --> R2
    A3 --> R2
    A4 --> R2
    R2 --> R3
    R2 --> R4
    R2 --> D4
    D2 --> D1
    D3 --> D1
    D4 --> D1
    D6 --> D1
    D1 --> D5
```

## 5. 数据流架构图

```mermaid
flowchart TD
    A[入口页面 / 搜索页 / 首页 / 本地缓存] --> B[collect_series_ids]
    B --> C[fetch_series_data]
    C --> D[data/raw/dongchedi]
    C --> E[extract_clean_series_record]
    E --> F[clean_record_to_markdown]
    E --> G[validate_clean_record]
    F --> H[governance orchestrator]
    H --> I{decision}
    I -->|accept| J[chunk_text]
    I -->|repair| J
    I -->|review| K[review queue]
    I -->|drop| L[skip processed]
    J --> M[validate_training_item]
    M --> N[data/processed JSONL]
    N --> O[Parquet exporter]
    N --> P[split_processed_assets]
    P --> Q1[rag_corpus]
    P --> Q2[training_corpus]
    P --> Q3[eval_candidates]
```

## 6. 治理链路图

```mermaid
flowchart LR
    A[clean_record + markdown_text] --> B[route]
    B --> C[rule_dedup]
    C --> D[semantic_dedup]
    D --> E[quality]
    E --> F[decision]
    F --> G[accept]
    F --> H[repair]
    F --> I[review]
    F --> J[drop]
```

### 6.1 节点职责

| 节点 | 主要职责 | 是否可用 LLM |
| --- | --- | --- |
| `route` | 识别样本渠道、模板、处理策略 | 是 |
| `rule_dedup` | 基于内容 hash / 规范化 hash 去重 | 否 |
| `semantic_dedup` | 基于相似度阈值做语义去重 | 否 |
| `quality` | 输出质量分、问题标签、修复建议、readiness | 是 |
| `decision` | 给出 `accept / repair / review / drop` | 否 |

### 6.2 治理结果落盘图

```mermaid
flowchart TD
    A[GovernanceOrchestrator] --> B[audit_logs]
    A --> C[governance_audit.jsonl]
    A --> D[governance_steps.jsonl]
    A --> E[governance_summary.json]
    A --> F[governance_failures.jsonl]
    A --> G[dedup_manifest.json]
    A --> H[semantic dedup store]
```

## 7. RAG 架构图

### 7.1 构建阶段

```mermaid
flowchart LR
    A[processed / rag_corpus JSONL] --> B[EmbeddingClient]
    B --> C[向量数组]
    C --> D[FaissVectorStore.build]
    D --> E[dongchedi.index]
    D --> F[dongchedi_records.jsonl]
    D --> G[dongchedi_meta.json]
    D --> H[DuckDB chunks/build_runs]
```

### 7.2 查询阶段

```mermaid
flowchart LR
    A[用户问题] --> B[EmbeddingClient.embed_query]
    B --> C{retrieval_mode}
    C -->|dense| D[FAISS search]
    C -->|hybrid| E[FAISS dense recall]
    C -->|hybrid| F[BM25 sparse recall]
    E --> G[RRF 融合]
    F --> G
    G --> H[term coverage 特征]
    H --> I[可选 reranker]
    I --> J[top_k hits]
    J --> K[build_context]
    K --> L[LLMClient]
    L --> M[最终回答 + 来源]
```

### 7.3 RAG 组件关系图

```mermaid
flowchart TB
    R1[rag_llm_demo.py] --> U1[EmbeddingClient]
    R1 --> U2[FaissVectorStore]
    R1 --> U3[RerankerClient]
    R1 --> U4[LLMClient]
    R1 --> U5[LightRAGStorage]
    U1 --> U2
    U2 --> U3
    U3 --> U4
    U2 --> U5
```

## 8. 评测与回流架构图

```mermaid
flowchart LR
    A[golden_set.jsonl] --> B[rag_ragas_eval.py]
    C[FAISS / records] --> B
    D[EmbeddingClient + LLMClient] --> B
    B --> E[ragas_results.json]
    E --> F[sync_ragas_feedback.py]
    F --> G[feedback_bridge.py]
    G --> H[ragas_feedback.jsonl]
    G --> I[review_queue.jsonl]
    G --> J[repair_queue.jsonl]
    E --> K[ci_eval_gate.py]
```

### 8.1 评测模式

```mermaid
flowchart TD
    A[rag_ragas_eval.py] --> B{eval_backend}
    B -->|ragas 可用| C[真实 ragas 评测]
    B -->|ragas 不可用| D[lightweight fallback]
    C --> E[faithfulness / answer_relevancy / context_precision / context_recall]
    D --> E
```

## 9. 多轮对话架构图

### 9.1 对话主流程

```mermaid
flowchart TD
    A[用户消息] --> B[ConversationMemoryStore.ensure_session]
    B --> C[add_message user]
    C --> D[list_messages]
    D --> E[merge_preferences]
    E --> F[get_task_memory]
    F --> G[classify_dialog_route]
    G --> H[rewrite_query]
    H --> I[build_memory_summary]
    I --> J[clarification decision]
    J -->|需要澄清| K[返回澄清问题]
    J -->|不需要澄清| L[RAGBackend.ask]
    L --> M[render_dialog_answer]
    M --> N[update_task_memory]
    N --> O[add_message assistant]
    O --> P[ResponsePayload]
```

### 9.2 记忆结构图

```mermaid
flowchart LR
    A[SQLite chat_memory.sqlite3] --> B[sessions]
    A --> C[messages]
    A --> D[preferences]
    A --> E[task_memory]
```

### 9.3 API 架构图

```mermaid
flowchart LR
    A[Web UI / TestClient / 外部调用方] --> B[FastAPI conversation/api.py]
    B --> C[MultiTurnChatService]
    C --> D[ConversationMemoryStore]
    C --> E[RAGBackend]
    E --> F[HashRAGBackend]
    E --> G[FaissRAGBackend]
```

## 10. 工作流架构图

```mermaid
flowchart LR
    A[run_pipeline_workflow.py] --> B[main]
    A --> C[build]
    A --> D[eval]
    A --> E[feedback]
    A --> F[ci_gate]
    A --> G[workflow state]
    A --> H[workflow alerts]
    A --> I[quality baseline]
```

### 10.1 状态文件内容

| 内容 | 说明 |
| --- | --- |
| `run_id` | 本次工作流 ID |
| `steps` | 每一步命令、耗时、返回码、输出摘要 |
| `resume_history` | 断点续跑历史 |
| `alerts` | 失败告警与质量漂移告警 |
| `results.success` | 本次工作流是否成功 |
| `results.failed_step` | 失败步骤名称 |

## 11. 模块职责速查表

| 模块 | 代表文件 | 核心职责 |
| --- | --- | --- |
| 抓取层 | `scraper/dcd_scraper.py` | 多入口收集车系 ID、抓取页面、保存 raw 数据 |
| 清洗层 | `cleaner/dcd_cleaner.py` | 规范化原始 JSON、生成 Markdown |
| 质量层 | `quality/data_quality.py` | 规则质检、质量报告、字段覆盖统计 |
| 治理层 | `agent_pipeline/orchestrator.py` | 编排 route/dedup/quality/decision |
| 资产分流 | `agent_pipeline/dataset_splitter.py` | 输出 `rag_corpus/training_corpus/eval_candidates` |
| RAG 层 | `rag_llm_demo.py` | build/query、混合检索、生成问答 |
| 评测层 | `rag_ragas_eval.py` | ragas 与 fallback 评测 |
| 回流层 | `agent_pipeline/feedback_bridge.py` | 低分样本分类回流 |
| 工作流层 | `scripts/run_pipeline_workflow.py` | 串联主流程并落状态与告警 |
| 对话层 | `conversation/service.py` | 多轮状态管理与统一对话入口 |
| 记忆层 | `memory/store.py` | SQLite 持久化 session/messages/preferences/task_memory |

## 12. 典型运行路径图

### 12.1 离线数据生产与 RAG 构建

```text
python main.py
-> python scripts/split_processed_assets.py
-> python rag_llm_demo.py build ...
-> python rag_ragas_eval.py ...
-> python scripts/sync_ragas_feedback.py ...
-> python scripts/ci_eval_gate.py ...
```

### 12.2 多轮服务启动

```text
python scripts/serve_multi_turn_api.py --backend-mode hash --host 127.0.0.1 --port 8001
-> FastAPI
-> /ui 前端页面
-> /docs Swagger
-> /api/v1/chat 多轮对话
```

## 13. 适合如何理解这个项目

可以从三个视角理解：

### 13.1 从数据工程视角

它是一个“原始网页 -> 高质量语料 -> 多类资产”的数据流水线。

### 13.2 从 RAG 视角

它是一个“语料治理 + 混合检索 + 评测回流”的 RAG 工程原型。

### 13.3 从应用视角

它是一个“带会话记忆与 query rewrite 的汽车问答/推荐服务”。

## 14. 结论

`Crawl_cc` 的价值不只在于代码功能数量，而在于它已经具备较完整的工程链路：

- 有真实数据入口
- 有清洗和治理
- 有索引和问答
- 有评测和回流
- 有工作流和验收
- 有多轮服务和前端

如果作为项目汇报材料，推荐优先展示本文档中的总览图、治理链路图、RAG 架构图和多轮对话流程图。

---

**文档版本**: v1.0  
**最后更新**: 2026-05-28
